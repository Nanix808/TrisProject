import admin_users from './admin_users';
import admin_transport from './admin_transport';

const admin_modules = {
  admin_users: admin_users,
  admin_transport: admin_transport,
};

export default admin_modules;
